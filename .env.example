# Required: Discord bot token (Discord Developer Portal → Your App → Bot → Token)
DISCORD_BOT_TOKEN=your_bot_token_here

# Required: Channel ID where the bot posts (right-click channel with Developer Mode on → Copy Channel ID)
DISCORD_CHANNEL_ID=1234567890123456789

# Optional: mention to ping (default: @everyone). Use @here or <@&ROLE_ID> or leave empty to disable
DISCORD_PING=@everyone

# Optional: minutes between checks when running in loop (default: 60)
CHECK_INTERVAL=60

# Optional: log level (DEBUG, INFO, WARNING, ERROR). Default: INFO
LOG_LEVEL=INFO

# DATABASE_URL is set by docker-compose for the app. For local runs, set it yourself, e.g.:
# DATABASE_URL=postgresql://seminar:seminar@localhost:5432/seminar
